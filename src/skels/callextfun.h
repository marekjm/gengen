/*
 * File automatically generated by
 * gengen 1.0 by Lorenzo Bettini 
 * http://www.gnu.org/software/gengen
 */

#ifndef CALLEXTFUN_GEN_CLASS_H
#define CALLEXTFUN_GEN_CLASS_H

#include <string>
#include <iostream>

using std::string;
using std::ostream;

class callextfun_gen_class
{
 protected:
  string name;

 public:
  callextfun_gen_class()
  {
  }
  
  callextfun_gen_class(const string &_name) :
    name (_name)
  {
  }

  static void
  generate_string(const string &s, ostream &stream, unsigned int indent)
  {
    if (!indent || s.find('\n') == string::npos)
      {
        stream << s;
        return;
      }

    string::size_type pos;
    string::size_type start = 0;
    string ind (indent, ' ');
    while ( (pos=s.find('\n', start)) != string::npos)
      {
        stream << s.substr (start, (pos+1)-start);
        start = pos+1;
        if (start+1 <= s.size ())
          stream << ind;
      }
    if (start+1 <= s.size ())
      stream << s.substr (start);
  }

  void set_name(const string &_name)
  {
    name = _name;
  }

  void generate_callextfun(ostream &stream, unsigned int indent = 0)
  {
    string indent_str (indent, ' ');
    indent = 0;
  
    stream << "if (record->";
    generate_string (name, stream, indent + indent_str.length ());
    stream << ")";
    stream << "\n";
    stream << indent_str;
    stream << "  generate_string (record->";
    generate_string (name, stream, indent + indent_str.length ());
    stream << ", stream, indent + strlen (indent_str));";
    stream << "\n";
    stream << indent_str;
    stream << "else";
    stream << "\n";
    stream << indent_str;
    stream << "  generate_";
    generate_string (name, stream, indent + indent_str.length ());
    stream << " (stream, record, indent + strlen (indent_str));";
  }
};

#endif // CALLEXTFUN_GEN_CLASS_H
